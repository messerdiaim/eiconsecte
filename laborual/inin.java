import java.util.*;

Calendar date = Calendar.getInstance();
date.set(Calendar.DAY_OF_MONTH, 1);
System.out.println(date.getTime()); // This will output the date with the day set to the 1st of the current month
